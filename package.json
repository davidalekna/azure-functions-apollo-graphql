{
  "name": "azure-nodejs",
  "version": "1.0.0",
  "description": "NodeJS Apollo GraphQL API",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "apollo-server-azure-functions": "^2.9.16"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.3",
    "typescript": "^3.7.5"
  }
}
